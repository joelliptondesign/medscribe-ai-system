# MED-SCRIBE

Non-clinical prototype for LangGraph-based medical documentation workflow exploration.

Setup: `python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt`

Architecture: Minimal LangGraph scaffold with placeholder node modules and a staged schema artifact.

